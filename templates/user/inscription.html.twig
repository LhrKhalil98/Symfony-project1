{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}

    <h1>Inscription</h1>
<div class="form ">   
    {{ form_start(form) }}

  <div class="row">

    
    <div class="form-group">{{ form_row(form.email) }}<span> </div> 
     <div class="form-group">{{ form_row(form.nom_prenom) }}</div>
    <div class="form-group"> {{ form_row(form.password) }}</div>
      </div>
   <button type="submit" class="btn btn-success">Ajouter</button>
     {{ form_end(form) }}

          </div>

      
 
        </div>

{% endblock %}
